# Leave both values blank until the Cloudflare Worker is configured and deployed.
# Push stays inactive unless both values contain real, non-placeholder values.
VITE_BMC_PUSH_API_URL=
VITE_BMC_VAPID_PUBLIC_KEY=

# Never put VAPID_PRIVATE_JWK or ADMIN_TOKEN in this Vite file.